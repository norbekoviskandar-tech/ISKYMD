# Archived one-off scripts

Debug and migration scripts moved out of the repo root. They are kept for
reference only. Many use relative paths (e.g. `./src/lib/...`) written for the
repo root and will need those paths adjusted before they can run again.
Supabase is the source of truth for schema and data; `schema-snapshots/` is
historical.
